import React from 'react';

const ProgressBar = (props) => (
	<div className="progress-page">
		<img src="img/preloader-black_2.gif" role="presentation" className="loader" />
	</div>
);

export default ProgressBar;
